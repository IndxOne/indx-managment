# Copier en .env.local (jamais commité) et renseigner les valeurs réelles.
# Récupérées via Supabase Dashboard > Project Settings > API (projet indxone-Hub,
# réutilisé pour INDXONE Projets — cf. décision de coût, table préfixées projets_*).
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
